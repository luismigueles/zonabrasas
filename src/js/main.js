// JS mínimo para interactividad opcional
document.addEventListener('DOMContentLoaded', () => {
  // Lazy loading de imágenes ya cubierto por loading="lazy" nativo
  console.log('WebLuismi - Web de comparativas');
});
