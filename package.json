{
  "name": "web-luismi",
  "version": "1.0.0",
  "description": "Web estática de recomendación y comparación de productos (afiliación)",
  "scripts": {
    "build": "node scripts/build/index.js",
    "ingest": "node scripts/ingest/index.js",
    "serve": "npx http-server dist -c-1 --port 3000",
    "dev": "node scripts/build/index.js --watch"
  },
  "devDependencies": {
    "ejs": "^3.1.10",
    "http-server": "^14.1.1"
  }
}
